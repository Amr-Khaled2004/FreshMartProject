PORT=5000
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/freshmart?retryWrites=true&w=majority
JWT_SECRET=replace_this_with_a_long_random_secret

# Optional SMTP settings for order confirmation emails
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_TLS_REJECT_UNAUTHORIZED=true
EMAIL_FROM=
